<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/left_options_creation"
        android:title="@string/previous_step"
        android:icon="@drawable/btn_flecheup"
         android:showAsAction="ifRoom"
         android:visible="false"/>
     <item android:id="@+id/right_options_creation"
        android:title="@string/next_step"
        android:icon="@drawable/btn_flechedown_desactivated"
         android:showAsAction="ifRoom"/>
</menu>
